<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<portlet-app xmlns="http://java.sun.com/xml/ns/portlet/portlet-app_2_0.xsd" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" version="2.0" xsi:schemaLocation="http://java.sun.com/xml/ns/portlet/portlet-app_2_0.xsd http://java.sun.com/xml/ns/portlet/portlet-app_2_0.xsd">

    <portlet>
        <portlet-name>FacsStatistics</portlet-name>
        <display-name>Statistics</display-name>
        
        <portlet-class>facs.ui.CustomVaadinPortlet</portlet-class>
        <init-param>
            <name>UI</name>
            <value>facs.ui.StatisticsUI</value>
        </init-param>
        
        <!--
          To enable displaying multiple Vaadin portlets on a page,
          they should all use the same widgetset. In that case, the
          widgetset can be configured on the portal level (parameter
          vaadin.widgetset) or here for each portlet.
        -->
        <!--
        <init-param>
            <name>widgetset</name>
            <value>com.vaadin.portal.gwt.PortalDefaultWidgetSet</value>
        </init-param>
        -->
        
        <!-- Supported portlet modes and content types. -->
        <supports>
            <mime-type>text/html</mime-type>
            <portlet-mode>view</portlet-mode>
            <!-- <portlet-mode>edit</portlet-mode> -->
            <!-- <portlet-mode>help</portlet-mode> -->
        </supports>
        
        <!-- Not always required but Liferay uses these. -->
        <portlet-info>
            <title>FacsStatistics</title>
            <short-title>FacsStatistics</short-title>
        </portlet-info>
    </portlet>
    
     <portlet>
        <portlet-name>FacsBooking</portlet-name>
        <display-name>Book</display-name>
        
        <portlet-class>facs.ui.CustomVaadinPortlet</portlet-class>
        <init-param>
            <name>UI</name>
            <value>facs.ui.BookUI</value>
        </init-param>
        
        <!--
          To enable displaying multiple Vaadin portlets on a page,
          they should all use the same widgetset. In that case, the
          widgetset can be configured on the portal level (parameter
          vaadin.widgetset) or here for each portlet.
        -->
        <!--
        <init-param>
            <name>widgetset</name>
            <value>com.vaadin.portal.gwt.PortalDefaultWidgetSet</value>
        </init-param>
        -->
        
        <!-- Supported portlet modes and content types. -->
        <supports>
            <mime-type>text/html</mime-type>
            <portlet-mode>view</portlet-mode>
            <!-- <portlet-mode>edit</portlet-mode> -->
            <!-- <portlet-mode>help</portlet-mode> -->
        </supports>
        
        <!-- Not always required but Liferay uses these. -->
        <portlet-info>
            <title>FacsBooking</title>
            <short-title>FacsBooking</short-title>
        </portlet-info>
    </portlet>   
    
      <portlet>
        <portlet-name>FacsSettings</portlet-name>
        <display-name>Settings</display-name>
        
        <portlet-class>facs.ui.CustomVaadinPortlet</portlet-class>
        <init-param>
            <name>UI</name>
            <value>facs.ui.SettingsUI</value>
        </init-param>
        
        <!--
          To enable displaying multiple Vaadin portlets on a page,
          they should all use the same widgetset. In that case, the
          widgetset can be configured on the portal level (parameter
          vaadin.widgetset) or here for each portlet.
        -->
        <!--
        <init-param>
            <name>widgetset</name>
            <value>com.vaadin.portal.gwt.PortalDefaultWidgetSet</value>
        </init-param>
        -->
        
        <!-- Supported portlet modes and content types. -->
        <supports>
            <mime-type>text/html</mime-type>
            <portlet-mode>view</portlet-mode>
            <!-- <portlet-mode>edit</portlet-mode> -->
            <!-- <portlet-mode>help</portlet-mode> -->
        </supports>
        
        <!-- Not always required but Liferay uses these. -->
        <portlet-info>
            <title>FacsSettings</title>
            <short-title>FacsSettings</short-title>
        </portlet-info>
    </portlet>      
    
</portlet-app>
